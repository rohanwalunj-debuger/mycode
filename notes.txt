pseudo-classes: Helps add extra styles

  when we hover :
    .subscribe-button:hover{
        background-color: green;
    }

  When we click the button :
    .subscribe-button:active{
      background-color: blue;
    }

------------------------------------------------------
opacity : how we see through
it takes the values between 0 and 1
1 means completely visible 
0 means completely gone
opacity means to fade out    

--------------------------------------------
transition: opacity 1s;
transition takes 2 values 
on what we want to transition and second the time
smoothly transitions the opacity
always put transition in parent styles
---------------------------------------------
 box-shadow: 10px 10px 10px black;
 it takes 4 values hori verti blur and color
 rgba(red, green, blue, alpha);
 alpha means fade out(opacity)